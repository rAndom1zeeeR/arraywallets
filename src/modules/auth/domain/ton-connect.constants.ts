/** Auth.js Credentials provider id (signIn first argument). */
export const TON_CREDENTIALS_PROVIDER_ID = "ton";

/** Prisma Account.provider value (matches ArrayTonV16 backend). */
export const TON_CONNECT_ACCOUNT_PROVIDER = "tonconnect";
