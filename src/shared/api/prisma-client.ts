/**
 * Single entry point for the generated Prisma Client (`generated/prisma`).
 * Import types, enums, `Prisma` namespace, and `PrismaClient` from here — not from `@generated/prisma/client`.
 *
 * @example
 * import { PrismaClient, Prisma, ChainActionType } from "@/shared/api/prisma-client";
 *
 * For the app singleton use `@/shared/api/prisma` (`prisma` instance).
 */

export * from "@generated/prisma/client";
